{
  "servers": {
    "supply": {
      "enabled": true,
      "command": "node",
      "args": ["dist/src/mcp/supply-server-entry.js"],
      "cwd": "${workspaceRoot}"
    },
    "supply-remote": {
      "enabled": false,
      "transport": "http",
      "urlEnv": "SUPPLY_REMOTE_URL",
      "apiKeyEnv": "SUPPLY_REMOTE_API_KEY"
    },
    "filesystem": {
      "enabled": true,
      "command": "cmd",
      "args": [
        "/c",
        "npx",
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "${workspaceRoot}"
      ],
      "cwd": "${workspaceRoot}"
    },
    "git": {
      "enabled": true,
      "command": "uvx",
      "args": [
        "--with",
        "mcp<2",
        "mcp-server-git",
        "--repository",
        "${workspaceRoot}"
      ],
      "cwd": "${workspaceRoot}"
    }
  }
}
